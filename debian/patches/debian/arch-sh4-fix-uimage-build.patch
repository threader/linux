From: Nobuhiro Iwamatsu <iwamatsu@nigauri.org>
Subject: [sh4] Fix uImage build
Bug-Debian: https://bugs.debian.org/569034
Forwarded: not-needed

[bwh: This was added without a description, but I think it is done
 only to avoid a build-dependency on u-boot-tools.]
---
 arch/sh/Makefile | 1 -
 1 file changed, 1 deletion(-)

Index: linux-master/arch/sh/Makefile
===================================================================
--- linux-master.orig/arch/sh/Makefile	2026-08-21 15:09:18.000000000 +0200
+++ linux-master/arch/sh/Makefile	2026-08-21 15:09:28.000000000 +0200
@@ -81,7 +81,6 @@ OBJCOPYFLAGS	:= -O binary -R .note -R .n
 
 # Give the various platforms the opportunity to set default image types
 defaultimage-y					:= zImage
-defaultimage-$(CONFIG_SH_SH7785LCR)		:= uImage
 defaultimage-$(CONFIG_SH_RSK)			:= uImage
 defaultimage-$(CONFIG_SH_URQUELL)		:= uImage
 defaultimage-$(CONFIG_SH_MIGOR)			:= uImage
